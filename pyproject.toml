[project]
name = "worked"
version = "0.1.0"
description = "Passive work-hours tracker for Hyprland, attributed per repo"
requires-python = ">=3.13"
dependencies = []

[dependency-groups]
dev = ["pytest==9.1.1"]

[tool.ruff]
line-length = 120
